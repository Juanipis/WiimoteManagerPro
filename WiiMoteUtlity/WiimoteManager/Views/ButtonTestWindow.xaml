<Window x:Class="WiimoteManager.Views.ButtonTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WiimoteManager"
        mc:Ignorable="d"
        Title="Wiimote Button Test Diagnostic"
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Window.Resources>
        <local:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3F3F46"/>
                    <Setter Property="Foreground" Value="#666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="Wiimote Button Mapping Diagnostic Tool"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Instructions Card -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📋 Instructions"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>
                <TextBlock Text="{Binding Instructions}"
                           TextWrapping="Wrap"
                           Foreground="#CCCCCC"/>
            </StackPanel>
        </Border>

        <!-- Current Test Status -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Current Test:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding CurrentTestButton}"
                               FontSize="18"
                               Foreground="#4EC9B0"
                               FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Last Detected:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding LastDetectedButtons}"
                               FontSize="16"
                               Foreground="#DCDCAA"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Raw Hex:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding LastRawHex}"
                               FontSize="16"
                               FontFamily="Consolas"
                               Foreground="#B5CEA8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Test Results -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Results Header -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="📊 Test Results"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="Completed: " Foreground="#CCCCCC"/>
                        <TextBlock Text="{Binding TestsCompleted}" FontWeight="Bold" Margin="0,0,15,0"/>
                        
                        <TextBlock Text="✓ " Foreground="#4EC9B0" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TestsCorrect}" Foreground="#4EC9B0" FontWeight="Bold" Margin="0,0,15,0"/>
                        
                        <TextBlock Text="✗ " Foreground="#F48771" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TestsIncorrect}" Foreground="#F48771" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>

                <!-- Results List -->
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding TestResults}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          Background="#252526"
                          Foreground="White"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#3F3F46"
                          RowBackground="#2D2D30"
                          AlternatingRowBackground="#252526">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Expected" Binding="{Binding ExpectedButton}" Width="100"/>
                        <DataGridTextColumn Header="Raw Hex" Binding="{Binding RawHex}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                    <Setter Property="Foreground" Value="#B5CEA8"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Detected Buttons" Binding="{Binding DetectedButtons}" Width="*"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCorrect}" Value="True">
                                            <Setter Property="Foreground" Value="#4EC9B0"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCorrect}" Value="False">
                                            <Setter Property="Foreground" Value="#F48771"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Grid Grid.Row="4" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Content="▶ Start Test"
                    Command="{Binding StartTestCommand}"
                    IsEnabled="{Binding IsTestRunning, Converter={StaticResource InverseBoolConverter}}"
                    Margin="0,0,10,0"
                    Padding="20,10"/>

            <Button Grid.Column="2"
                    Content="⏹ Stop Test"
                    Command="{Binding StopTestCommand}"
                    IsEnabled="{Binding IsTestRunning}"
                    Background="#CE5C00"
                    Margin="0,0,10,0"
                    Padding="20,10"/>

            <Button Grid.Column="3"
                    Content="📁 Open Logs"
                    Command="{Binding OpenLogsFolderCommand}"
                    Background="#3F3F46"
                    Padding="20,10"/>
        </Grid>
    </Grid>
</Window>
